source_id: federal_pbs_programs_all
title: Commonwealth PBS program expenses (Table 2.1 family)
publisher: Commonwealth of Australia
jurisdiction: Commonwealth
government_level: federal
source_family: budget
measure_type: budget_estimate
accounting_basis: accrual
estimate_status: budget
period_granularity: financial_year
input:
  path: data/staging/breakdowns/pbs_programs_all.csv
  format: csv
columns:
  financial_year: fy
  amount_aud: amount
  node_name: category
  estimate_status: estimate_status
  locator: locator
  landing_url: landing_url
  original_resource_url: resource_url
  cached_copy_path: cached_copy_path
attribution:
  landing_url_column: landing_url
  original_resource_url_column: resource_url
  # Per-row: each fact's own source PBS PDF, not a single fixed document - see
  # duckdb_etl.py's per_row_path_column handling. The literal path below is
  # only a fallback for any row missing this column (should not happen).
  cached_copy_path: data/raw/federal/federal_transparency_portal/snapshots/20260722T043324Z/files/2025-26-Treasury-PBS.pdf
  cached_copy_path_column: cached_copy_path
fact_key_template: '{source_id}|{financial_year}|{node_name}|{measure_type}|{estimate_status}'
