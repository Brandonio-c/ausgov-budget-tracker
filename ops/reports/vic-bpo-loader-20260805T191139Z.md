# VIC BPO load and semantic validation (Task 4)

Generated: 2026-08-05T19:11:39Z.

## Backup

`scripts/ops/backup_facts_db.py` run immediately before any write:
`/home/vibe-server/backups/ausgov-budget-tracker/facts-20260805T190954Z.db`
(pre-load counts: 288,635 facts, 222,519 nodes, 128 source_documents).

## First load pass (`--apply`)

```
rows_extracted: 100
rows_quarantined_by_extractor: 0
rows_validated_publishable: 22
rows_quarantined_by_loader: 78
facts_already_present_idempotent_skip: 0
facts_to_insert: 22
revision_conflicts_quarantined: 0
nodes_inserted: 11
```

78 quarantined by the loader: every other real line item on the 3
sheets not in the 11-measure scope (2 `excluded_duplicate_label` -
`Total equity` and `Comprehensive result` - plus `unrecognized_label`
for every other row, e.g. `Employee benefits`, `Payables`, `Receipts
from Government`, etc., correctly left unpublished this milestone).

## Post-first-pass counts

| metric | before | after | delta |
|---|---:|---:|---:|
| facts | 288,635 | 288,657 | +22 |
| nodes | 222,519 | 222,530 | +11 |
| fact_nodes | 288,635 | 288,657 | +22 |
| source_documents | 128 | 129 | +1 |

11 nodes for 11 measure_types - matches exactly.

## Second load pass (`--apply` again) - idempotency check

```
rows_extracted: 100
rows_quarantined_by_extractor: 0
rows_validated_publishable: 22
rows_quarantined_by_loader: 78
facts_already_present_idempotent_skip: 22
facts_to_insert: 0
revision_conflicts_quarantined: 0
nodes_inserted: 0
```

**Post-second-pass counts: identical to post-first-pass** (facts
288,657; nodes 222,530; fact_nodes 288,657; source_documents 129).
`SELECT fact_key, COUNT(*) FROM facts GROUP BY fact_key HAVING COUNT(*) >
1` returns **zero rows**. All Task 4 idempotency criteria met: new
facts 0, new nodes 0, new edges 0, semantic changes 0.

## Integrity and coverage checks, immediately after both passes

- `task9_sql_integrity_checks.py`: `hard_failures: 0`, `orphan_facts: 0`,
  `orphan_nodes: 0`.
- `ingestion_coverage_audit.py`: `fully_ingested` 48 -> **49** (+1, this
  source), `adapter_missing` 168 -> **167** (-1). Every other status
  count byte-identical to the prior state.
- `ingestion_coverage_lineage.py`: 7 datasets, unchanged (this family is
  not part of the canonical-datasets lineage registry).
- `quarantine_report.py`: `quarantined=36417` - unchanged (this family's
  quarantine goes to its own separate file,
  `data/staging/quarantine/vic_bpo_load_quarantine.jsonl`, never
  `facts_pending_attribution`).

## Confirmed: existing annual dashboard totals unchanged

Queried the local production API directly after the load:

| query | value | unchanged from pre-load? |
|---|---:|---|
| `mode=actuals&level=federal&year=2024-25` | 745,030,000,000 | yes |
| `mode=actuals&level=state&year=2024-25` | 553,464,488,764.247 | yes (VIC's own whole-of-government total - confirms the new department-level `vic_bpo_*` facts did not get pulled into it, same as vic_afs_* before it) |

No `vic_bpo_*` compatibility_group is registered under any
`mode_to_family` mapping (`config/compatibility/view_families.yaml`), so
neither query above could have picked up any `vic_bpo_*` fact even in
principle.

## Next

Task 5: expose this family through the existing GFS/jurisdiction
explorer, as a fourth toggle option alongside Expenses/Liabilities/VIC
AFS - not a new dedicated page, same reasoning as VIC AFS.
