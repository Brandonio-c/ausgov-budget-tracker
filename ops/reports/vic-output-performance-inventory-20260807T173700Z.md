# VIC Output Performance workbook selection

Generated: 2026-08-07T17:37:00Z.

The acquired workbook is valid structured data with eight sheets: one
introduction and seven output sheets. Across the output sheets it contains 77
performance-measure rows: 26 number, 38 per cent, two date, one percentage
variance, two percentage-point variance, one ratio, and seven `$ million`
rows.

The selected internally consistent cluster is the seven `Total output cost`
rows, one per output sheet. Each has a numeric 2024-25 actual and target,
yielding 14 facts. The target is stored as `estimate_status=budget`; it is the
source's planned output cost, not a revised estimate. Native AUD millions are
scaled once to AUD.

The other 70 rows remain explicitly deferred as contextual performance KPIs.
They mix counts, dates, percentages, ratios, threshold strings such as `≥95`
and `≤5.0`, and `n/a`. They must not be coerced to dollars or collapsed into a
single generic quantity measure. The selected output costs stay in a dedicated
compatibility group and are not inserted into whole-of-government additive
trees.
