# TAS GGS Key Fiscal Measures: load and validate (Task 6)

Generated: 2026-08-06T17:29:06Z.

## Backup

`data/facts.db` backed up via `scripts/ops/backup_facts_db.py` to
`/home/vibe-server/backups/ausgov-budget-tracker/facts-20260806T172805Z.db`
before any write - counts recorded in the backup's own report
(288,675 facts, 222,539 nodes, 129 source_documents, 36,417
quarantined). `backup_matches_source_bytes: false` despite identical
file size and identical row counts - an artifact of SQLite's backup
API producing a different on-disk page layout (VACUUM/freelist
ordering), not a data-loss concern; confirmed by matching sizes and
downstream count checks below.

## First `--apply`

```
python3 scripts/ingest/reload_tas_ggs_key_fiscal_measures.py --apply
```

160/160 facts published (16 financial years x 10 measures), 10 nodes
inserted, 0 rows quarantined by the loader (the extractor itself
quarantined 0 rows against the real workbook - every cell parsed
cleanly).

`facts`: 288,675 -> 288,835 (+160). `nodes`: 222,539 -> 222,549 (+10).
`fact_nodes`: 288,675 -> 288,835 (+160). `measure_definitions`: 71 -> 81
(+10, migration 011 applied automatically by the loader's own
`migrate()` call).

Spot-checked several facts directly against the source workbook: e.g.
`tas_ggs_cash_surplus_deficit` actual 2016-17 = 829,000,000 AUD
(matches the source's `829` $m exactly, including the footnote-marker-
stripped year label); `tas_ggs_expense` actual 2016-17 =
5,674,000,000 AUD.

## Second `--apply` (idempotency proof)

```
python3 scripts/ingest/reload_tas_ggs_key_fiscal_measures.py --apply
```

`facts_to_insert: 0`, `nodes_inserted: 0`,
`facts_already_present_idempotent_skip: 160`,
`revision_conflicts_quarantined: 0` - byte-identical database state,
zero duplicate fact_keys.

## Integrity / coverage / lineage / quarantine

| check | result |
|---|---|
| `scripts/ops/task9_sql_integrity_checks.py` | `hard_failures: 0`, `orphan_facts: 0`, `orphan_nodes: 0`, `orphan_edges: 0` |
| `scripts/ingest/ingestion_coverage_audit.py` | `fully_ingested: 49 -> 50` (+1, this new source transitioning from `adapter_missing`), `adapter_missing: 167 -> 166` (-1) - the expected transition for a genuinely new family, unlike the additional-measures-on-an-already-ingested-source case from the prior VIC BPO SOCE/Admin milestone |
| `scripts/ingest/ingestion_coverage_lineage.py` | 7 datasets - unchanged (this tracker is driven by `config/lineage/canonical_datasets.yaml`, not automatically by new adapters) |
| `scripts/ingest/quarantine_report.py` | `quarantined=36417` - unchanged (this loader's own quarantine file is a separate file not included in that report's scan list, consistent with every other family's loader-specific quarantine file) |

## Dashboard contamination check

`scripts/ops/dashboard_api_audit.py` against the real, bind-mounted
production backend (`http://127.0.0.1:8010`) immediately after the
load: **`total_hard_failures: 0`, `total_accepted_source_rounding_
warnings: 0`** across all 6 required paths and 7 PBS crosswalk cases -
identical to the pre-load clean state. None of the 10 new measure_types
is registered under any `mode_to_family` mapping
(`config/compatibility/view_families.yaml`), so this family cannot
reach any existing dashboard tree even in principle.

## Next

Task 7: expose the new measures (wire into an existing explorer -
likely the GFS/jurisdiction explorer's pattern, adding a new "TAS GGS"
toggle mirroring the VIC AFS/BPO/SOCE-Admin design - or document why a
different placement is a better fit).
