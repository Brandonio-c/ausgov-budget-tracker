# QLD Report on State Finances: load and validate (Task 6)

Generated: 2026-08-07T14:40:03Z.

## Backup

`data/facts.db` backed up via `scripts/ops/backup_facts_db.py` to
`/home/vibe-server/backups/ausgov-budget-tracker/facts-20260807T143911Z.db`
before any write - counts recorded in the backup's own report
(288,877 facts, 222,549 nodes, 130 source_documents, 36,417
quarantined).

## First `--apply`

```
python3 scripts/ingest/reload_qld_report_on_state_finances.py --apply
```

112/112 facts published (8 measures x 7 editions x 2 estimate_status),
8 nodes inserted (a genuinely new family - no pre-existing `qld_rsf_*`
nodes to reuse), 0 rows quarantined by the loader (all 7 rows the
extractor itself quarantined were the expected narrative "Revenue"
false positives, not loader-level rejections), 0 revision conflicts.

`facts`: 288,877 -> 288,989 (+112). `nodes`: 222,549 -> 222,557 (+8).
`fact_nodes`: 288,877 -> 288,989 (+112). `measure_definitions`: 81 ->
89 (+8, migration 012 applied automatically by the loader's own
`migrate()` call).

Every new fact's `amount_aud` verified directly against Task 3's
manually-inspected source values (e.g. `qld_rsf_revenue` 2018-19
actual = 59,834,000,000 AUD, matching the source's `59,834` $m
exactly; `qld_rsf_fiscal_balance` 2019-20 estimated_actual =
-9,318,000,000 AUD).

## Second `--apply` (idempotency proof)

```
python3 scripts/ingest/reload_qld_report_on_state_finances.py --apply
```

`facts_to_insert: 0`, `nodes_inserted: 0`,
`facts_already_present_idempotent_skip: 112`,
`revision_conflicts_quarantined: 0` - byte-identical database state,
zero duplicate fact_keys.

## Integrity / coverage / lineage / quarantine

| check | result |
|---|---|
| `scripts/ops/task9_sql_integrity_checks.py` | `hard_failures: 0`, `orphan_facts: 0`, `orphan_nodes: 0` |
| `scripts/ingest/ingestion_coverage_audit.py` | `fully_ingested: 50 -> 51` (+1, this genuinely new source transitioning from `adapter_missing`), `adapter_missing: 166 -> 165` (-1) - the expected transition for a new family |
| `scripts/ingest/ingestion_coverage_lineage.py` | 7 datasets (unchanged dataset count); `facts_source_keys_observed: 127 -> 128` (+1, the new `qld_report_on_state_finances_actuals` source_key) |
| `scripts/ingest/quarantine_report.py` | `quarantined=36417` - unchanged (this loader's own quarantine file is a separate file not included in that report's scan list) |

## Dashboard contamination check

`scripts/ops/dashboard_api_audit.py` against the real, bind-mounted
production backend (`http://127.0.0.1:8010`) immediately after the
load: **`total_hard_failures: 0`, `total_accepted_source_rounding_
warnings: 0`** across all 6 required paths and 7 PBS crosswalk cases -
identical to the pre-load clean state. QLD state actuals total
confirmed unchanged (`553,464,488,764.25`, byte-identical to every
prior milestone's baseline). None of the 8 new `qld_rsf_*`
compatibility_groups is registered under any `mode_to_family` mapping,
so this family cannot reach any existing dashboard tree.

## Next

Task 7: expose the new family - wire into the existing GFS/
jurisdiction explorer as a new "QLD RSF" toggle, mirroring the VIC AFS/
BPO/TAS GGS pattern.
