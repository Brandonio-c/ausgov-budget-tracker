# MFS test coverage (Task 10)

Generated: 2026-08-05T04:10:13Z.

## New test files

| file | count | covers |
|---|---:|---|
| `tests/ingest/test_mfs_loader.py` (new) | 20 | YTD period calculation (`month_to_period_end`/`financial_year_start`, incl. leap-year Feb), stock vs flow period semantics (`period_start=None` for stocks), label-to-measure mapping (`build_label_index`, incl. ambiguous-mapping rejection), stable fact keys (`build_fact_key`), negative-amount preservation, citation preservation through a real `--apply` load, missing-source-file/undetermined-unit/unrecognized-label quarantine, and the core isolation guarantee: every `mfs_*` measure has its own `compatibility_group`, none shared with `actual_expense`/`budget_expense`/`gfs_revenue`/`gfs_liability` |
| `tests/api/test_mfs_api.py` (new) | 12 | `/v2/mfs/measures` (all 15), `/v2/mfs/series` (financial_year + reporting_month filtering, unknown-measure 400), `/v2/mfs/years`, `/v2/mfs/compare` (valid pair, stock+flow 422 rejection, unknown-measure 400, too-many-measures 400), stock facts get no `period_start`, every fact carries a citation, and **the core non-negotiable proof**: an annual `/v2/dashboard/tree` total is identical before and after every MFS fact is deleted from the same database |
| `src/frontend/tests-e2e/mfs-explorer.spec.ts` (new) | 4 | page loads with the flow disclosure badge; switching the measure selector to a stock flips the disclosure to point-in-time; all 4 required visualizations render and no expenditure pie/sunburst exists on the page; the citation panel shows a real citation for the selected reporting month |

Plus what Tasks 4/6 already added:
`tests/ingest/test_mfs_aggregates.py` (pre-existing, 4 tests: `$m`/`$b`
unit scaling, non-YTD/bare-month quarantine, footnote-row/marker
stripping, citation locator completeness) and
`tests/ingest/test_mfs_revision_policy.py` (Task 6, 3 tests: idempotent
identical reload, revision-conflict quarantine with the original value
preserved, a genuinely new month distinguished from a conflicting one).

## Mission's required coverage list, mapped to actual tests

| requirement | test(s) |
|---|---|
| `$m` and `$b` unit conversion | `test_mfs_aggregates.py::test_unit_scaling_dollars_b_and_m` |
| negative and parenthesized amounts | `test_mfs_loader.py::test_classify_preserves_negative_amount` (+ Task 4's real-corpus finding of 1,156 negative rows, confirmed correctly parsed) |
| YTD period calculation | `test_mfs_loader.py::test_month_to_period_end[*]` (6 cases incl. leap year), `test_financial_year_start` |
| stock reporting dates | `test_mfs_loader.py::test_classify_stock_gets_no_period_start`, `test_mfs_api.py::test_stock_fact_has_no_period_start` |
| bare-month ambiguity quarantine | `test_mfs_aggregates.py::test_non_ytd_column_quarantined_not_guessed` |
| label-to-measure mapping | `test_mfs_loader.py::test_build_label_index_covers_every_real_label_variant`, `test_build_label_index_rejects_ambiguous_duplicate_mapping` |
| flow/stock incompatibility | `test_mfs_api.py::test_compare_rejects_stock_and_flow_mix` |
| annual-versus-YTD incompatibility | `test_mfs_loader.py::test_every_mfs_measure_has_a_dedicated_compatibility_group_distinct_from_annual` |
| stable fact keys | `test_mfs_loader.py::test_build_fact_key_is_stable_and_identity_complete` |
| duplicate source discovery | `test_mfs_revision_policy.py` (all 3) |
| revision precedence | `test_mfs_revision_policy.py::test_reload_with_changed_amount_is_quarantined_not_silently_overwritten` |
| idempotent reload | `test_mfs_revision_policy.py::test_reload_of_identical_data_is_idempotent` |
| citation preservation | `test_mfs_loader.py::test_loaded_fact_preserves_full_citation`, `test_mfs_api.py::test_citation_present_on_every_fact` |
| annual root totals unchanged | `test_mfs_api.py::test_annual_dashboard_total_unaffected_by_mfs_facts` (deletes every MFS fact mid-test and confirms the annual total is byte-identical - proves the totals were never touched, not just coincidentally unaffected) |
| API filtering | `test_mfs_api.py::test_series_filters_by_financial_year`, `test_series_filters_by_reporting_month` |
| frontend period disclosure | `mfs-explorer.spec.ts` (both tests checking the flow/stock disclosure badges) |

## Fixtures

All new Python tests use **synthetic fixture databases**
(`schema_migrate.migrate()` against `tmp_path`, or purpose-built
`openpyxl` workbooks reduced to the minimum necessary cells) - none touch
the real `data/facts.db`. The Playwright spec runs against
`scripts/ingest/build_e2e_fixture_db.py`'s small, committed fixture
(extended this task with a small `mfs_ytd_revenue` + `mfs_stock_total_assets`
series), verified locally against the real static export + a real
backend before being wired into CI's `e2e` job
(`.github/workflows/ci.yml`, alongside the existing `dashboard.spec.ts`).

## A small fix while widening ruff's scope for this check

Running `ruff check` across `scripts/ops` (to validate the new test
files' companion scripts) surfaced an unused `sys` import in
`scripts/ops/cleanup_stray_mfs_preload.py` (Task 1) - removed. Also
surfaced several **pre-existing** issues in `tests/api/*.py` (unused
imports, import-order) unrelated to this milestone and outside CI's
actual checked ruff scope (`scripts src/backend tests/unit tests/ops
tests/ingest` - `tests/api` was never included) - left untouched as
out-of-scope, pre-existing debt, not silently absorbed into this
milestone's changes.

## Full suite result

`python -m pytest tests -q`: **296 passed** (was 261 at Task 1's
baseline; +3 from Task 6's `test_mfs_revision_policy.py`, +32 from this
task's `test_mfs_loader.py` (20) and `test_mfs_api.py` (12) - the CI
test-registry fix in Task 8 does not change the total collected count).
`ruff check` across CI's actual checked scope: clean.

## Next

Task 11: full validation - the complete required command list (pytest,
`task9_sql_integrity_checks.py`, `ingestion_coverage_audit.py`,
`ingestion_coverage_lineage.py`, `quarantine_report.py`,
`revenue_reconciliation.py`, `debt_reconciliation.py`, frontend
lint/build/`test:e2e`), plus a fresh semantic dashboard audit to confirm
every existing path is still clean after the MFS load.
